# ============================================
# EcoPilot — Environment Variables (TEMPLATE)
# Copier ce fichier en .env et remplir les valeurs
# ============================================

# --- PostgreSQL ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-strong-postgres-password

# --- Keycloak Admin ---
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=your-strong-keycloak-admin-password

# --- Keycloak Client Secret ---
# À copier depuis: Keycloak Console → Realm ecopilot → Clients → ecopilot-backend → Credentials
KEYCLOAK_CLIENT_SECRET=eiJKCUvMjhcMCoG6FXS2BhMskslkoill

# --- Keycloak URLs ---
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=ecopilot
KEYCLOAK_JWKS_URI=http://keycloak:8080/realms/ecopilot/protocol/openid-connect/certs

# --- Grafana ---
GF_SECURITY_ADMIN_PASSWORD=your-grafana-admin-password

# --- PgAdmin ---
PGADMIN_DEFAULT_EMAIL=admin@ecopilot.com
PGADMIN_DEFAULT_PASSWORD=your-pgadmin-password

# --- MongoDB ---
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-mongo-password
